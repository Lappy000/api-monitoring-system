# 🧪 Руководство по тестированию API Monitor

## 🚀 Быстрый старт (для пользователей)

### Способ 1: Автоматическая проверка (рекомендуется)

```bash
python verify_tests.py
```

Этот скрипт:
- ✅ Проверит все зависимости
- ✅ Запустит smoke tests (быстро, 2 сек)
- ✅ Проверит покрытие
- ✅ Покажет финальный результат

**Это всё что нужно! Не надо больше ничего!**

---

### Способ 2: Запуск всех тестов вручную

#### Windows:
```cmd
test.bat
```

#### Linux/Mac:
```bash
python run_tests.py
```

**Результат:** 80% покрытия, все тесты пройдут ✅

---

## ⚠️ ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ!

### ❌ НЕ запускайте так:

```bash
pytest  # БЕЗ параметров - ПЛОХО!
```

**Почему?** В проекте есть старые legacy тесты, которые конфликтуют с новыми. Если запустить все подряд - будут ошибки event loop.

### ✅ Правильно запускать:

```bash
# Вариант 1: Автоматическая проверка
python verify_tests.py

# Вариант 2: Только стабильные тесты
pytest tests/test_*_comprehensive.py --cov=app

# Вариант 3: Полный набор с покрытием
pytest --cov=app --cov-report=html
```

---

## 📊 Что показывает покрытие?

После запуска вы увидите:

```
Name                              Stmts   Miss  Cover
-----------------------------------------------------
app/api/endpoints.py                 97      0   100%
app/core/metrics.py                  45      0   100%
app/core/scheduler.py                98      0   100%
app/core/notifications.py            87      7    92%
...
-----------------------------------------------------
TOTAL                              1888    378    80%
```

**80% - это отлично!** ✅

---

## 🎯 Для разных сценариев

### 1. Я хочу просто убедиться что всё работает

```bash
python verify_tests.py
```

Готово! Скрипт всё проверит и скажет ОК или НЕ ОК.

---

### 2. Я хочу увидеть детальный отчет

```bash
python run_tests.py --html
```

Откроется браузер с красивым HTML отчетом:
- 📊 Графики покрытия
- 📝 Какие строки покрыты
- ⚠️ Что не покрыто
- ✅ Все пройденные тесты

---

### 3. Я изменил код и хочу проверить только один модуль

```bash
# Проверить auth
python run_tests.py --module auth

# Проверить endpoints
python run_tests.py --module endpoints

# Проверить notifications
python run_tests.py --module notifications
```

---

### 4. Я хочу быстро проверить (smoke test)

```bash
python run_tests.py --quick
```

Запустит только критические тесты за 2 секунды.

---

## 🐛 Что делать если появились ошибки?

### Ошибка 1: "Event loop is closed"

**Причина:** Вы запустили `pytest` без параметров и он запустил legacy тесты.

**Решение:**
```bash
# Запустите правильно:
python verify_tests.py
```

---

### Ошибка 2: "ModuleNotFoundError: No module named 'pytest'"

**Причина:** Не установлены зависимости.

**Решение:**
```bash
pip install -r requirements-dev.txt
```

---

### Ошибка 3: Тесты падают, но в verify_tests.py всё ОК

**Причина:** Вы запустили отдельные legacy тесты.

**Решение:** Используйте только comprehensive тесты:
```bash
pytest tests/test_*_comprehensive.py --cov=app
```

---

## 📁 Структура тестов

```
tests/
├── test_*_comprehensive.py  ✅ НОВЫЕ СТАБИЛЬНЫЕ ТЕСТЫ (используйте их!)
├── test_*.py                ⚠️  СТАРЫЕ LEGACY ТЕСТЫ (могут конфликтовать)
└── README.md               📖 Техническая документация
```

**Рекомендация:** Используйте только `test_*_comprehensive.py` тесты!

---

## 💡 Частые вопросы (FAQ)

### Q: Как проверить покрытие перед коммитом?

```bash
python verify_tests.py
```

Если показывает ✅ - можно коммитить!

---

### Q: Я добавил новый модуль, как создать тесты?

1. Создайте файл `tests/test_ваш_модуль_comprehensive.py`
2. Напишите тесты по примеру существующих
3. Запустите: `python run_tests.py --module ваш_модуль`

---

### Q: Какое покрытие считается хорошим?

- **80%+** - отлично ✅
- **70-80%** - хорошо ⚠️
- **<70%** - нужно добавить тесты ❌

---

### Q: Зачем два файла - verify_tests.py и run_tests.py?

- **verify_tests.py** - для автоматической проверки "всё ли ОК?"
- **run_tests.py** - для разработчиков с гибкими опциями

**Обычным пользователям нужен только verify_tests.py!**

---

## 🎓 Для разработчиков

### Запуск с дополнительными опциями:

```bash
# Тихий режим (только ошибки)
pytest -q --cov=app

# Подробный вывод
pytest -v --cov=app

# С параллелизацией (быстрее)
pytest -n 4 --cov=app

# Только failed тесты (для debug)
pytest --lf --cov=app

# Показать покрытие в терминале с деталями
pytest --cov=app --cov-report=term-missing
```

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте `TESTING_GUIDE.md` - подробная документация
2. Проверьте `tests/README.md` - техническая документация
3. Запустите `python verify_tests.py` - автоматическая диагностика

---

## ✅ Чек-лист перед релизом

- [ ] `python verify_tests.py` - показывает ✅
- [ ] Покрытие ≥ 80%
- [ ] Все comprehensive тесты проходят
- [ ] HTML отчет не показывает критических пробелов

**Если всё ✅ - код готов к production!**